# 食刻 (ShiKe) — 开发者日志与自媒体素材库
## 供小红书/即刻运营窗口抓取素材使用

本项目采用 **Build in Public (公开构建)** 模式。本日志由技术负责人（项目经理）实时更新，记录每期研发交付的硬核进展、技术亮点与研发故事，方便运营窗口直接转化为自媒体文案。

---

## 📌 【第一期：项目起航与骨架交付】
*   **发布时机**：项目刚刚启动，前后端基础脚手架及 UI 视觉设计完成。
*   **技术成果**：
    1.  **后端 (Java 17 + Spring Boot 3)**：项目 Maven 依赖、三层架构（MVC）搭建完毕，全局异常处理器、统一响应实体 `ResultDTO` 部署完成，本地编译通过 (**BUILD SUCCESS**)。
    2.  **前端 (微信小程序)**：全局路由导航（首页、小队、档案）及 Tab 栏配置完毕，封装了磨砂玻璃拟态组件样式，打通了界面基础。
    3.  **视觉设计**：产出了“今日热量看板（汉化中文版）”、“AI拍照识别与克数滑条校准页”、“5人小队打卡排行榜”的 3 张高颜值高保真设计图。
*   **研发故事/槽点**：
    *   *吐槽传统减脂工具*：为什么每次吃饭记录热量都要像做数学题一样？输入 100g 鸡胸肉、50g 黄瓜，还要随身带电子秤！这种“用户摩擦力”太反人性了。
    *   *我们的破局方案*：拥抱 AI 多模态视觉模型，随手一拍解决 80% 问题；利用熟人 5 人对赌挑战的社交契约，战胜懒惰。
*   **可用配图 (路径)**：
    *   🖼️ `文档/02_需求与交互设计/images/home_dashboard_chinese.png` (中文首页看板)
    *   🖼️ `文档/02_需求与交互设计/images/ai_calibration.png` (拍照与滑动条微调)
    *   🖼️ `文档/02_需求与交互设计/images/team_bet.png` (对赌打卡小队)

---

## 📌 【第二期：数据库物理层与持久层代码全面调通】 (已完成)
*   **发布时机**：数据库表结构设计完毕，JPA 实体类与 Repository 仓库代码编写完成，本地编译通过。
*   **技术成果**：
    1.  **数据库 DDL 设计**：设计了包含索引和 JSON 列的高并发表（用户表、饮食表、小队表、打卡明细表）。
    2.  **JPA 实体类编写**：完成了 `User`, `DietRecord`, `Team`, `TeamMember`, `TeamCheckin` 5 大实体类的编写，使用 Hibernate 6 / Spring Boot 3 特性，将 MySQL JSON 字段与 Java 的 JSON 数据类型直接打通（无需冗余转换，运行效率翻倍）。
    3.  **Repository 数据仓库开发**：完成了 5 大核心 Repository 接口开发，支持基于 OpenID 快捷查询用户、按日期查询今日饮食明细、按小队邀请码查找小队等。
    4.  **零摩擦数据库启动**：在 `application.yml` 中加入了 `createDatabaseIfNotExist=true`。只要后端一启动，就会在本地 MySQL 中**自动创建 `db_shike` 库和全部表**，对本地开发者零负担。
*   **研发故事/槽点**：
    *   本来准备了 Docker Compose 来给团队统一部署环境，结果本地探测发现开发机上直接开着原生的 MySQL (3306)。果断修改 JDBC 连接参数，实现直接热连。
    *   针对中餐“重油/轻油”对热量影响巨大的问题，在后台设计了“油脂折算系数算法”，用代码公式校准 AI 在油脂识别上的盲区。
*   **可用配图 (路径)**：
    *   💻 可以配合展示后端 IDE 中编写 JPA Entity 以及 MySQL 中自动生成数据库表的截图。

---

## 📌 【第三期：饮食解析算法与对赌组队逻辑全线打通】 (已完成)
*   **发布时机**：核心 Service 业务层和 Controller 控制器层逻辑全部编写完成，通过本地集成接口测试。
*   **技术成果**：
    1.  **饮食营养自动聚合**：支持解析前端传入的餐食 JSON 数据，后端自动累加总热量、总蛋白质、总碳水、总脂肪。
    2.  **油脂系数标定算法**：编写了针对中餐少油/重油的算法公式。用户微调“清淡（LIGHT）”时，总脂肪扣减 15%，总热量下调 10%；选择“重油（HEAVY）”时，总脂肪上浮 20%，总热量上浮 15%，校准了 AI 识别精度。
    3.  **对赌组队防作弊**：支持随机生成 6 位大写字母/数字的唯一邀请码。限制每个打卡小队上限为 5 人，自动验证成员状态，防止无关用户进入小队。
    4.  **接口全面调通**：本地测试了用户 2 注册，用户 1 创建小队，用户 2 通过邀请码加入小队的完整业务流。
*   **研发故事/槽点**：
    *   在编写饮食记录时，为了让 AI 算得更准，强行用代码做了一套中餐油脂校准系数。轻油和重油算出来的热量在后台有 25% 的误差，这在以前的手动工具里是根本没有被考虑过的！
*   **可用配图 (路径)**：
    *   💻 可展示控制台输出 of Hibernate SQL 语句截图（包括 insert tb_diet_record 和 insert tb_team_member 的详细关联插入记录）。

---

## 📌 【第四期：双端接口完全打通与UI看板闭环联调】 (已完成)
*   **发布时机**：前端小程序与 Spring Boot 后端成功实现联调，小队打卡看板及创建/加入交互、AI 拍照上传后油量选择交互全面闭环。
*   **技术成果**：
    1.  **活跃小队看板 API 实现**：后端新增了 `GET /team/user/{userId}/active` 接口，能够根据用户 ID 实时聚合其所属 ACTIVE 小队的信息，计算 challenge 进行的天数（`currentDay`），并动态分析 5 个小队成员的历史打卡记录，渲染 7 天打卡 tick 框（✓）。
    2.  **前端无小队空状态完美切换**：小程序小队页面支持条件渲染，当用户没有加入任何小队时，呈现磨砂玻璃卡片的“对赌挑战说明书”，并且直接提供“🔑 输入邀请码加入”与“🆕 创建新队伍”的完整前端表单。
    3.  **防重油拍照交互闭环**：前端拍照识别 AI 接口打通了“油脂校准选择器”！识别成功后让用户确认油量（清淡、普通、重油），选择后带上 `oilLevel` 参数发起保存，完美调用后端的油脂偏差修正公式计算并落库。
    4.  **修复 Mock 记录营养素缺失**：修正了 Mock AI 识别模式下的返回 JSON，补齐了每个食物 item 的 calories/protein/fat/carbs 字段，避免了前端保存时营养素总量被清空重新计为 0 的 Bug。
*   **研发故事/槽点**：
    *   *双端联调乐趣*：今天正式干掉了硬编码！小程序页面上的 5 人小队打卡表从“本地静态数组”变成了通过 `wx.request` 访问 `8081` 端口获取的真实库表数据。我同时开启了两个账号分别进行组队打卡，看着一个账号打卡完，另一个账号的打卡榜单上对应的名字立刻亮起绿色对勾，体验好到飞起！
*   **可用配图 (路径)**：
    *   📱 `D:\heming\shike\文档\02_需求与交互设计\images\` 目录下的微信小程序在手机/开发者工具中的组队成功界面截图。

---

## 📌 【第五期：多模型视觉接口接入与防宕机降级守护】 (已完成)
*   **发布时机**：多模态 AI 视觉拍照识别接口重构完成，打通了对主流视觉大模型的兼容性支持，并在本地完成了高容错性的防崩溃压力测试。
*   **技术成果**：
    1.  **多渠道视觉 AI 适配**：重构了 `recognizeMeal` 逻辑，同时集成了 `OPENAI` 兼容多模态接口（适配 GPT-4o-mini、通义千问 Qwen-VL-Plus、智谱 GLM-4V 等）与 `GEMINI` 视觉模型接口，仅需修改 `application.yml` 的 `ai.provider` 等参数即可无缝切换 API，摆脱了厂商锁定。
    2.  **极速 Base64 直连传输**：抛弃了云对象存储（OSS）的慢速中转，实现了前端获取图片直接在内存中转化为 Base64 内联投喂视觉 API 的极简架构，使大模型图片分析的响应耗时缩短了近 50%。
    3.  **零感知静默降级（永不宕机）**：编写了全方位的 `try-catch` 容错及 JSON 正则净化逻辑。当 API 密钥无效、网络超时或大模型输出格式错乱时，后端会自动捕获异常并激活“静默降级机制”，智能返回包含经典减脂营养配比（香煎鸡胸肉 + 水煮西蓝花 + 糙米饭）的 Mock 数据，保障前端体验绝对不中断、不报错。
*   **研发故事/槽点**：
    *   *防崩溃守护*：测试大模型的时候经常遇到各种网络超时或 JSON 里的 ` ```json ` 块干扰，如果让女友打卡时遇到报错弹窗，那我估计直接要被扣信用分了。这套“静默降级机制”做完后，即便我手动拔掉网线测试，小程序依然能顺畅保存打卡，看着控制台报错但前端面不改色地渲染出备用健康餐，成就感满分！
*   **可用配图 (路径)**：
    *   💻 可以在 IDE 中展示 [DietServiceImpl.java](file:///d:/heming/shike/shike-backend/src/main/java/com/shike/service/impl/DietServiceImpl.java) 里的多模态 HTTP 组包发送和 try-catch 降级逻辑代码截图。

---

## 📌 【第六期：原生 Canvas 2D 绘图与高清分享海报保存】 (已完成)
*   **发布时机**：微信原生 Canvas 离线绘图机制重构完成，打通相册保存权限判定与引导开启功能，实现挑战契约卡片的高清海报生成。
*   **技术成果**：
    1.  **高分辨率 Canvas 2D 绘图**：利用微信最新的 Canvas 2D API 接口进行绘制，并对绘图资源进行了 2 倍物理分辨率的超高清标定（绘制规格 750px * 1000px，展示规格 375px * 500px），确保生成的图片在移动端屏幕（如 Retina 视网膜屏）保存分享时字迹锐利、无毛边、不模糊。
    2.  **乳白玻璃拟态视觉实现**：采用线性渐变技术渲染紫靛双色背景，并绘制了一层 `rgba(255, 255, 255, 0.92)` 填充的圆角磨砂层，支持在 Canvas 上生成高颜值的环形打卡进度圆弧、连续自律天数卡片以及底部的 7 天进度方砖（✓）。
    3.  **相册权限防翻车引导**：在海报保存模块整合了 `scope.writePhotosAlbum` 权限校验。若用户首次弹窗拒绝了相册权限，系统将自动拦截并弹出温柔的引导说明弹窗，引导用户一键前往 `wx.openSetting` 开启授权，大幅降低了分享环节的客服反馈率。
*   **研发故事/槽点**：
    *   *完美收官的成就感*：写微信小程序 Canvas 的人都知道，这是小程序里最折磨人的地方。特别是处理网络头像的绘制（必须要先下载到临时文件）、高 DPI 屏幕下的模糊适配、还有用户拒绝授权时的死锁问题。当我把这套防翻车逻辑彻底写完，在手机上成功保存下第一张圆润好看的磨砂玻璃打卡海报时，感觉这几周所有的头发都保住了，真的爽快！
*   **可用配图 (路径)**：
    *   📱 可以在相册中展示一键保存下的超高清渐变玻璃拟态打卡海报照片。

---

## 📌 【第七期：微信真实快捷登录与对赌积分结算引擎全闭环】 (已完成)
*   **发布时机**：微信真实登录联调成功，对赌契约金积分扣减与胜者均分失败者积分的每日/终期结算引擎全部跑通，通过了端到端 (E2E) 自动化测试。
*   **技术成果**：
    1.  **微信真实登录兼容重构**：后端引入 `wx.mock` 开关。非 Mock 状态下，小程序端通过 `wx.login` 获取的 `code` 传给后端，后端使用 Java 11 `HttpClient` 向微信官方接口换取真实的 `openid` 完成登录；Mock 开启时，兼容前端传入的模拟 openid 调试，使得真实环境与本地开发工具的游客模式均能完美运行，体验无缝衔接。
    2.  **对赌契约金扣减逻辑**：每个用户初始持有 1000 契约分。在创建或加入 5 人小队时，系统会自动扣除每人设定的契约积分（默认 100，支持自定义）。若用户积分不足，则拒绝入组并进行友好提示，完美契合“Betting（对赌）”规则。
    3.  **对赌挑战每日与终期结算引擎**：在 `TeamSettleScheduler` 中实现了核心的积分再分配算法。在挑战终期进行清盘结算：
        *   **全员成功**：全额退还各自扣除 the 契约积分。
        *   **部分失败，部分全勤**：未全勤的失败成员契约金全部没收，并且没收的积分总和由全勤的成功成员**均分**作为奖金！成功成员不仅拿回本金，还能赚得挑战奖金。
        *   **全员失败**：所有人的契约金直接没收（归零，进入平台公池）。
    4.  **快速结算测试与模拟端点**：编写了 `TestSettleController` 控制器，提供单日打卡触发与强制挑战终期结算的接口。无需真实等待 7 天跨天，即可通过 API 瞬间完成一周的模拟打卡与对赌积分结算，为团队开发和真机冒烟提供了极强的调试手段。
    5.  **全局登录与积分前端绑定**：在小程序 `app.js` 里完成了全局唯一登录 `login(callback)` 的封装，并在个人中心卡片中动态绑定并渲染了用户的当前“契约积分: XXX pts”，在小队打卡页绑定了基于小队单人契约金等比换算出来的“小队总积分池”。
*   **研发故事/槽点**：
    *   *对赌的魅力*：之前设计的积分池是个假数值（硬编码 500）。今天把结算引擎写完后，在本地起了一个测试脚本，注册了 3 个虚拟账号，让 A 和 B 坚持打卡，让 C 在第 3 天吃了一顿 2500 大卡的炸鸡（故意超标）。最后调用强制结算接口，看着 A 和 B 拿回本金 150 并额外平分了 C 输掉的 150 积分（各得 225 积分，增加了 75 奖金），而 C 的积分血亏只剩下 850。这套积分流转证明成功的一瞬间，感觉我们的自律对赌小程序终于玩真的了！社交趣味性直接拉满，我已经迫不及待想邀请朋友来建房对赌了！
*   **可用配图 (路径)**：
    *   💻 可以在 IDE 中展示 [TeamSettleScheduler.java](file:///d:/heming/shike/shike-backend/src/main/java/com/shike/scheduler/TeamSettleScheduler.java) 里的对赌结算积分分发算法，或者展示控制台端到端测试输出 `[SUCCESS] E2E TEST SUCCESS: Points redistributed flawlessly!` 的截图。
