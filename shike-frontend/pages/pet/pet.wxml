<!-- pages/pet/pet.wxml -->
<view class="pet-immersive-page">

  <!-- 顶部状态栏与品牌（莫兰迪温润风格） -->
  <view class="top-nav-bar flex align-center justify-between">
    <view class="flex align-center nav-brand-wrap" style="gap: 12rpx;">
      <text style="font-size: 38rpx;">🐾</text>
      <view class="flex flex-col">
        <text class="brand-title">自律搭子乐园</text>
        <text class="brand-sub">温暖陪伴 · 科学轻盈变好</text>
      </view>
    </view>
    
    <view class="flex align-center nav-actions-wrap" style="gap: 8rpx;">
      <!-- 🎨 换场景入口 -->
      
      <!-- 📸 小红书自律拍立得入口 -->
      <view class="polaroid-nav-pill flex align-center" bindtap="onOpenPolaroidModal" wx:if="{{hasPet}}">
        <text style="font-size: 20rpx; margin-right: 4rpx;">📸</text>
        <text class="pill-text-rose">拍立得</text>
      </view>
      <!-- 形态图鉴入口 -->
      <view class="stage-dex-pill flex align-center" bindtap="onOpenDexModal" wx:if="{{hasPet}}">
        <text style="font-size: 20rpx; margin-right: 4rpx;">📖</text>
        <text class="pill-text-indigo">图鉴</text>
      </view>
      <!-- 勋章馆入口 -->
      <view class="badge-nav-pill flex align-center" bindtap="onOpenBadgeModal" wx:if="{{hasPet}}">
        <text style="font-size: 20rpx; margin-right: 4rpx;">🏅</text>
        <text class="pill-text-amber">{{unlockedBadgeCount}}/6</text>
      </view>
      <!-- 刷新按钮 -->
      <view class="refresh-pill flex align-center justify-center" bindtap="onRefreshPage">
        <text style="font-size: 20rpx; color: #10B981; font-weight: 700;">🔄</text>
      </view>
    </view>
  </view>

  <!-- 状态 0：特性开关已关闭 -->
  <block wx:if="{{!petSystemEnabled}}">
    <view class="disabled-state-box flex flex-col align-center justify-center">
      <view class="disabled-icon-wrap">
        <text style="font-size: 80rpx;">🐾</text>
        <view class="disabled-lock-badge">🔒</view>
      </view>
      <text class="disabled-title">自律搭子维护中</text>
      <text class="disabled-desc">搭子小窝正在精心升级装扮中，敬请期待稍后回归！</text>
      
      <button class="btn-back-home" bindtap="onGoHome">
        🏠 返回首页看板
      </button>
    </view>
  </block>

  <!-- 页面主体加载态 -->
  <block wx:elif="{{loading}}">
    <view class="loading-container flex flex-col align-center justify-center">
      <view class="magic-spinner"></view>
      <text class="loading-text">正在唤醒你的自律闺蜜搭子...</text>
    </view>
  </block>

  <!-- 状态 A：未领养状态（Finch 原生萌宠直选与领养向导） -->
  <block wx:elif="{{!hasPet}}">
    <view class="adopt-showcase-flow">
      
      <!-- 引导标语 -->
      <view class="adopt-banner-card">
        <view class="flex align-center" style="gap: 10rpx; margin-bottom: 8rpx;">
          <text style="font-size: 32rpx;">✨</text>
          <text style="font-size: 30rpx; font-weight: 900; color: #0F172A;">选择你的专属自律闺蜜搭子</text>
        </view>
        <text class="banner-desc">
          每位认真自律的女孩子，都有一只专属小精灵。每天记录饮食、运动与喝水，陪你一起变轻、变美、变从容！
        </text>
      </view>

            <!-- 🌟 Finch 3D 浮岛直选展台（直接展示活生生的软萌幼宠） -->
      <view class="adopt-3d-stage-box flex flex-col align-center">
        <!-- 3D 沉浸式阳光原木自律小屋背景 -->
        <!-- 3D 浮空岛微缩世界预览背景 -->
        
        <!-- 搭子初见台词气泡 -->
        <view class="adopt-speech-bubble">
          <text class="adopt-bubble-text">“{{currentTypeInfo.quote}}”</text>
          <view class="adopt-bubble-arrow"></view>
        </view>

        <view class="stage-3d-viewport flex align-center justify-center">
          <view class="ambient-sunbeam"></view>
          
          <!-- 幼宠直观立绘与轻柔呼吸动画 -->
          <view class="adopt-pet-anchor pet-breathe-anim" bindtap="onTapPreviewPet">
            <image class="adopt-pet-img" src="{{currentTypeInfo.stages[0].image}}" mode="aspectFit" />
            <view class="pet-dynamic-shadow"></view>
          </view>
        </view>

        <view class="type-capsule" style="background: {{currentTypeInfo.themeBg}}; color: {{currentTypeInfo.themeColor}};">
          <text>{{currentTypeInfo.name}} · {{currentTypeInfo.tag}}</text>
        </view>
      </view>

      <!-- 4 大萌宠直观选择栏 (Finch 样式) -->
      <view class="type-switch-panel">
        <text class="panel-label">点击挑选与你有缘的搭子：</text>
        <view class="type-grid-finch">
          <view wx:for="{{types}}" wx:key="type" 
                class="finch-pet-pill {{selectedType === item.type ? 'active' : ''}}" 
                bindtap="onSelectType" data-type="{{item.type}}">
            <text class="pill-icon">{{item.icon}}</text>
            <view class="flex flex-col">
              <text class="pill-name">{{item.name}}</text>
            </view>
            <view class="pill-check" wx:if="{{selectedType === item.type}}">✓</view>
          </view>
        </view>
      </view>

      <!-- 起名面板 -->
      <view class="name-edit-panel">
        <text class="panel-label">给新搭子起个好听的爱称：</text>
        <view class="input-row flex align-center">
          <input class="name-ipt" type="text" placeholder="例如：木木 / 泡泡 / 饭团" value="{{petName}}" bindinput="onInputName" maxlength="12" />
          <view class="dice-btn" bindtap="onRandomName">
            <text>🎲 换一个</text>
          </view>
        </view>

        <view class="candidate-row flex align-center">
          <text style="font-size: 22rpx; color: #94A3B8;">常用萌名：</text>
          <view wx:for="{{candidateNames}}" wx:key="*this" class="tag-name" bindtap="onSelectCandidateName" data-name="{{item}}">
            {{item}}
          </view>
        </view>
      </view>

      <!-- 直接领养按钮 -->
      <view class="adopt-btn-wrap">
        <button class="btn-3d-adopt" bindtap="onDirectAdopt" loading="{{adopting}}">
          💖 领养 {{petName || currentTypeInfo.name}} · 开启温暖陪伴
        </button>
      </view>

    </view>
  </block>

  <!-- 状态 B：已领养状态（莫兰迪 3D 浮岛主舞台） -->
  <block wx:else>
    <view class="living-stage-wrapper">
      
      <!-- 顶部轻量浮动 HUD -->
      <view class="floating-hud-bar flex align-center justify-between">
        <view class="flex align-center" style="gap: 12rpx;">
          <text class="hud-pet-name">{{pet.name}}</text>
          <text class="hud-species-tag">{{currentStageInfo.name}}</text>
          <view class="hud-level-tag">Lv.{{pet.level}}</view>
        </view>
        <view class="stage-rank-tag stage-rank-{{petStageRank}}">{{currentStageInfo.stageTitle}}</view>
      </view>

            <!-- 🌟 真正 2.5D 卡通 3D 浮空岛微缩世界 (WebGL 硬件加速 + 真实深度遮挡) -->
      <view class="virtual-3d-world">
        
        <!-- WebGL 3D 交互画布 -->
        <canvas type="webgl" id="petWorldCanvas" class="pet-3d-webgl-canvas"
                bindtouchstart="onWorldTouchStart"
                bindtouchmove="onWorldTouchMove"
                bindtouchend="onWorldTouchEnd"
                bindtap="onTapWorldCanvas">
        </canvas>

        <!-- 3D 视差拖拽指引角标 -->
        <view class="parallax-hint-badge">
          <text class="hint-icon">👆</text>
          <text class="hint-text">滑动体验 3D 空间视差</text>
        </view>

        <!-- 🌟 悬浮动态闺蜜台词气泡（带实时思考 Loading 动效） -->
        <view class="speech-bubble-3d animate-bubble-float {{aiThinking ? 'bubble-thinking' : ''}}">
          <view class="flex align-center" wx:if="{{aiThinking}}">
            <text class="bubble-msg" style="color: #EC4899;">💭 搭子正在组织温暖的语言...</text>
          </view>
          <block wx:else>
            <text class="bubble-msg">“{{petDialogue || pet.dialogue}}”</text>
          </block>
          <view class="bubble-arrow"></view>
        </view>

        <!-- 飞行动画食物 (投喂抛物线) -->
        <view class="flying-food-projectile {{isFeeding ? 'animate-food-parabola' : ''}}" wx:if="{{isFeeding}}">
          <text class="food-emoji">{{foodIcon}}</text>
        </view>

        <!-- 浮动 EXP 飘字 -->
        <view class="floating-exp-gain {{isFeeding ? 'animate-exp-up' : ''}}" wx:if="{{isFeeding}}">
          <text class="exp-text">+10 能量 ✨</text>
        </view>

        <!-- 🛋️ 3D 浮空岛点位智能漫步导览条 -->
        <view class="room-spot-dock-bar flex align-center justify-center">
          <view wx:for="{{roomSpots}}" wx:key="id" 
                class="spot-dock-item {{currentSpotId === item.id ? 'active' : ''}}"
                bindtap="onSelectRoomSpot" data-id="{{item.id}}">
            <text class="dock-icon">{{item.icon}}</text>
            <text class="dock-name">{{item.name}}</text>
          </view>
        </view>

        <!-- 心情胶囊栏与陪伴天数 -->
        <view class="mood-bar-row flex align-center justify-center" style="gap: 14rpx;">
          <view class="mood-pill mood-{{pet.mood}} flex align-center">
            <text class="mood-icon">{{pet.mood === 'HAPPY' ? '😄' : (pet.mood === 'WAITING' ? '💭' : (pet.mood === 'HUNGRY' ? '🥺' : '🌸'))}}</text>
            <text class="mood-text">{{pet.moodText}}</text>
          </view>
          <view class="hud-streak-tag flex align-center">
            <text style="font-size: 22rpx; margin-right: 4rpx;">🔥</text>
            <text style="font-size: 20rpx; font-weight: 800; color: #EA580C;">连续自律第 {{pet.streakDays}} 天</text>
          </view>
        </view>

        <!-- 互动指引 -->
        <view class="touch-hint-label">
          <text>👆 轻触抚摸它，它会结合你今日真实自律数据温暖回应</text>
        </view>

      </view>

      <!-- 🎋 自律心声与闺蜜互动（受控合规模式） -->
      <view class="treehole-chat-deck">
        <view class="flex justify-between align-center" style="margin-bottom: 16rpx;">
          <view class="flex align-center" style="gap: 8rpx;">
            <text style="font-size: 30rpx;">🎋</text>
            <text style="font-size: 26rpx; font-weight: 900; color: #0F172A;">自律心声 · 听听搭子怎么说</text>
          </view>
          <text style="font-size: 20rpx; color: #EC4899; font-weight: 700;">闺蜜专属夸夸 ✨</text>
        </view>
        <text style="font-size: 22rpx; color: #64748B; margin-bottom: 16rpx; display: block;">选择你此刻的自律状态或小情绪，让搭子为你充充电：</text>

        <!-- 丰富的情绪与自律心声胶囊矩阵 -->
        <view class="interactive-chips-grid">
          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="今天按时完成了运动燃脂，快夸夸我！">
            <text class="chip-emoji">🏃</text>
            <text class="chip-title">运动打卡求表扬</text>
          </view>

          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="今天喝了好多水，感觉身体很轻盈！">
            <text class="chip-emoji">💧</text>
            <text class="chip-title">饮水充足求夸奖</text>
          </view>

          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="今天工作好累好想偷懒，快给我一点坚持下去的动力！">
            <text class="chip-emoji">🥺</text>
            <text class="chip-title">今天好累求抱抱</text>
          </view>

          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="晚上特别嘴馋想吃高热量炸鸡宵夜，快帮我稳住心态！">
            <text class="chip-emoji">🍗</text>
            <text class="chip-title">嘴馋想吃宵夜</text>
          </view>

          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="按时吃了健康的减脂餐，打卡完毕！">
            <text class="chip-emoji">🥗</text>
            <text class="chip-title">吃完减脂餐打卡</text>
          </view>

          <view class="chip-action-btn" bindtap="onTapQuickPrompt" data-prompt="准备睡觉啦，晚安！明天也要一起加油哦！">
            <text class="chip-emoji">🌙</text>
            <text class="chip-title">互道晚安美容觉</text>
          </view>
        </view>
      </view>

      <!-- 游戏化数值仪表盘（莫兰迪温润卡片） -->
      <view class="gamified-stats-deck">
        
        <!-- 饱食度 -->
        <view class="stat-gauge-cell">
          <view class="flex justify-between align-center" style="margin-bottom: 10rpx;">
            <view class="flex align-center" style="gap: 10rpx;">
              <text style="font-size: 28rpx;">🍎</text>
              <text class="gauge-name">饱食度 (随时间自然代谢，去惩罚零压力)</text>
            </view>
            <text class="gauge-number" style="color: {{pet.fullness < 30 ? '#F43F5E' : '#10B981'}};">{{pet.fullness}} / 100</text>
          </view>
          <view class="gauge-track">
            <view class="gauge-fill" style="width: {{pet.fullness}}%; background: {{pet.fullness < 30 ? '#F43F5E' : 'linear-gradient(90deg, #34D399, #10B981)'}};"></view>
          </view>
        </view>

        <!-- 成长进度与进化预览 -->
        <view class="stat-gauge-cell" style="margin-top: 24rpx;">
          <view class="flex justify-between align-center" style="margin-bottom: 10rpx;">
            <view class="flex align-center" style="gap: 10rpx;">
              <text style="font-size: 28rpx;">🌸</text>
              <text class="gauge-name">成长蜕变 (距离下一阶段还差 {{pet.maxExp - pet.exp}} 能量)</text>
            </view>
            <text class="gauge-number" style="color: #EC4899;">{{pet.exp}} / {{pet.maxExp}}</text>
          </view>
          <view class="gauge-track">
            <view class="gauge-fill" style="width: {{(pet.exp / pet.maxExp) * 100}}%; background: linear-gradient(90deg, #F472B6, #EC4899);"></view>
          </view>
          <view class="evolution-next-tip flex align-center justify-between" style="margin-top: 10rpx;">
            <text style="font-size: 20rpx; color: #64748B;">成长目标：{{nextStageGoalText}}</text>
            <text style="font-size: 20rpx; font-weight: 700; color: #EC4899;">{{petStageProgressText}}</text>
          </view>
        </view>

        <!-- 亲密度与背包 -->
        <view class="stat-pills-row flex justify-between" style="margin-top: 24rpx;">
          <view class="pill-box flex align-center justify-center">
            <text style="font-size: 30rpx; margin-right: 8rpx;">❤️</text>
            <text style="font-size: 24rpx; color: #475569;">闺蜜亲密度: <text style="font-weight: 900; color: #E11D48;">{{pet.intimacy}}</text></text>
          </view>
          <view class="pill-box flex align-center justify-center">
            <text style="font-size: 30rpx; margin-right: 8rpx;">🎒</text>
            <text style="font-size: 24rpx; color: #475569;">健康零食袋: <text style="font-weight: 900; color: #10B981;">{{pet.foodCount}}</text> 份</text>
          </view>
        </view>

      </view>

      <!-- 核心 3D 操作按键区 -->
      <view class="action-deck-card flex flex-col">
        <view class="flex justify-between" style="gap: 18rpx;">
          
          <button class="btn-3d-action btn-3d-feed flex align-center justify-center" bindtap="onFeedPet" disabled="{{isFeeding || pet.foodCount <= 0}}">
            <text style="font-size: 34rpx; margin-right: 10rpx;">🥣</text>
            <text style="font-weight: 900;">{{pet.foodCount > 0 ? '投喂小甜心 (' + pet.foodCount + ')' : '暂无食物'}}</text>
          </button>

          <button class="btn-3d-action btn-3d-checkin flex align-center justify-center" bindtap="onDailyCheckin" disabled="{{foodTasks.checkin}}">
            <text style="font-size: 34rpx; margin-right: 10rpx;">📅</text>
            <text style="font-weight: 900;">{{foodTasks.checkin ? '今日已签到' : '每日签到领粮'}}</text>
          </button>

        </view>
      </view>

      <!-- 🎒 每日自律赚粮任务中心 -->
      <view class="food-tasks-card">
        <view class="flex justify-between align-center" style="margin-bottom: 16rpx;">
          <view class="flex align-center" style="gap: 10rpx;">
            <text style="font-size: 32rpx;">🎒</text>
            <text style="font-size: 28rpx; font-weight: 900; color: #0F172A;">每日自律赚粮任务</text>
          </view>
          <text style="font-size: 22rpx; color: #10B981; font-weight: 800;">今日已赚 {{earnedFoodCount}}/5 份</text>
        </view>
        <text style="font-size: 22rpx; color: #64748B; margin-bottom: 20rpx; display: block;">完成日常自律打卡，免费为搭子带回专属营养粮！</text>

        <view class="task-item-list">
          
          <!-- 1. 每日签到 -->
          <view class="task-row flex justify-between align-center">
            <view class="flex align-center" style="gap: 14rpx;">
              <text class="task-icon">📅</text>
              <view class="flex flex-col">
                <text class="task-title">每日自律签到</text>
                <text class="task-reward">+1 份营养粮 · 开启元气一天</text>
              </view>
            </view>
            <button class="btn-task {{foodTasks.checkin ? 'done' : 'action'}}" bindtap="onDailyCheckin" disabled="{{foodTasks.checkin}}">
              {{foodTasks.checkin ? '✓ 已领取' : '一键签到'}}
            </button>
          </view>

          <!-- 2. 运动锻炼 -->
          <view class="task-row flex justify-between align-center">
            <view class="flex align-center" style="gap: 14rpx;">
              <text class="task-icon">🏃</text>
              <view class="flex flex-col">
                <text class="task-title">自律运动打卡</text>
                <text class="task-reward">+1 份营养粮 · 快走/瑜伽/慢跑</text>
              </view>
            </view>
            <button class="btn-task {{foodTasks.exercise ? 'done' : 'action'}}" bindtap="onGoExercise">
              {{foodTasks.exercise ? '✓ 已达成' : '去运动'}}
            </button>
          </view>

          <!-- 3. 三餐饮食 -->
          <view class="task-row flex justify-between align-center">
            <view class="flex align-center" style="gap: 14rpx;">
              <text class="task-icon">🍽️</text>
              <view class="flex flex-col">
                <text class="task-title">记录三餐轻食</text>
                <text class="task-reward">+1 份营养粮 · 拍照或记餐</text>
              </view>
            </view>
            <button class="btn-task {{foodTasks.diet ? 'done' : 'action'}}" bindtap="onGoDiet">
              {{foodTasks.diet ? '✓ 已达成' : '去记餐'}}
            </button>
          </view>

          <!-- 4. 喝水打卡 -->
          <view class="task-row flex justify-between align-center">
            <view class="flex align-center" style="gap: 14rpx;">
              <text class="task-icon">💧</text>
              <view class="flex flex-col">
                <text class="task-title">健康喝水打卡</text>
                <text class="task-reward">+1 份营养粮 · 水水润润一整天</text>
              </view>
            </view>
            <button class="btn-task {{foodTasks.water ? 'done' : 'action'}}" bindtap="onGoWater">
              {{foodTasks.water ? '✓ 已达成' : '去喝水'}}
            </button>
          </view>

          <!-- 5. 体重记录 -->
          <view class="task-row flex justify-between align-center">
            <view class="flex align-center" style="gap: 14rpx;">
              <text class="task-icon">⚖️</text>
              <view class="flex flex-col">
                <text class="task-title">记录晨起体重</text>
                <text class="task-reward">+1 份营养粮 · 见证变轻变好</text>
              </view>
            </view>
            <button class="btn-task {{foodTasks.weight ? 'done' : 'action'}}" bindtap="onGoWeight">
              {{foodTasks.weight ? '✓ 已达成' : '去记录'}}
            </button>
          </view>

        </view>
      </view>

    </view>
  </block>

  <!-- 📸 核心新功能：小红书自律拍立得弹窗 (Polaroid Postcard Modal) -->
  <view class="polaroid-modal-backdrop {{showPolaroidModal ? 'show' : ''}}" bindtap="onClosePolaroidModal">
    <view class="polaroid-modal-sheet" catchtap="noBubble">
      
      <view class="sheet-header flex justify-between align-center">
        <view class="flex align-center" style="gap: 10rpx;">
          <text style="font-size: 36rpx;">📸</text>
          <text class="sheet-title">今日份自律拍立得</text>
        </view>
        <text class="sheet-close" bindtap="onClosePolaroidModal">✕</text>
      </view>
      <text class="sheet-subtitle">打卡的美好瞬间，保存分享小红书与朋友圈～</text>

      <!-- 拍立得卡片本体 (经典复古胶带相框) -->
      <view class="polaroid-card-frame flex flex-col align-center">
        <view class="polaroid-tape"></view>
        
        <view class="polaroid-photo-box flex align-center justify-center">
          <image class="polaroid-pet-img" src="{{activePetImage}}" mode="aspectFit"></image>
          <view class="polaroid-stamp">自律轻盈 ✨</view>
        </view>

        <!-- 手写体自律金句 -->
        <view class="polaroid-caption-wrap flex flex-col align-center">
          <text class="polaroid-handwriting">“{{polaroidQuote}}”</text>
          <text class="polaroid-date">{{polaroidDateText}} · 和{{pet.name}}在一起的第 {{pet.streakDays}} 天</text>
        </view>

        <!-- 底部轻量健康数据气泡 -->
        <view class="polaroid-stats-capsules flex align-center justify-center">
          <view class="p-cap flex align-center">
            <text style="font-size: 20rpx; margin-right: 4rpx;">🔥</text>
            <text style="font-size: 20rpx; font-weight: 700; color: #EA580C;">连续自律 {{pet.streakDays}} 天</text>
          </view>
          <view class="p-cap flex align-center">
            <text style="font-size: 20rpx; margin-right: 4rpx;">🌸</text>
            <text style="font-size: 20rpx; font-weight: 700; color: #EC4899;">{{currentStageInfo.stageTitle}}</text>
          </view>
          <view class="p-cap flex align-center">
            <text style="font-size: 20rpx; margin-right: 4rpx;">🎒</text>
            <text style="font-size: 20rpx; font-weight: 700; color: #10B981;">已赚 {{earnedFoodCount}} 份零食</text>
          </view>
        </view>
      </view>

      <!-- 保存到相册按钮 -->
      <button class="btn-save-polaroid" bindtap="onSavePolaroidToAlbum">
        💖 保存拍立得到相册 · 去小红书晒单
      </button>

    </view>
  </view>

  <!-- 🌟 3阶形态进化图鉴弹窗 -->
  <view class="dex-modal-backdrop {{showDexModal ? 'show' : ''}}" bindtap="onCloseDexModal">
    <view class="dex-modal-sheet" catchtap="noBubble">
      
      <view class="sheet-header flex justify-between align-center">
        <view class="flex align-center" style="gap: 10rpx;">
          <text style="font-size: 36rpx;">📖</text>
          <text class="sheet-title">搭子成长蜕变图鉴</text>
        </view>
        <text class="sheet-close" bindtap="onCloseDexModal">✕</text>
      </view>
      <text class="sheet-subtitle">坚持自律打卡，见证搭子从破壳萌新到蜕变闺蜜的足迹！</text>

      <view class="dex-stage-cards">
        <view wx:for="{{currentTypeInfo.stages}}" wx:key="rank" 
              class="dex-card {{petStageRank >= item.rank ? 'unlocked' : 'locked'}} {{petStageRank === item.rank ? 'current' : ''}}">
          
          <view class="dex-card-top flex justify-between align-center">
            <view class="flex align-center" style="gap: 8rpx;">
              <text class="dex-rank-badge">阶段 {{item.rank}}</text>
              <text class="dex-stage-name">{{item.name}}</text>
            </view>
            <view class="dex-status-pill {{petStageRank >= item.rank ? 'active' : ''}}">
              {{petStageRank === item.rank ? '🌟 当前陪伴' : (petStageRank > item.rank ? '✓ 已解锁' : item.reqText)}}
            </view>
          </view>

          <view class="dex-avatar-preview flex align-center justify-center">
            <image class="dex-avatar-img {{petStageRank < item.rank ? 'silhouette' : ''}}" src="{{item.image}}" mode="aspectFit"></image>
          </view>

          <text class="dex-stage-desc">{{item.desc}}</text>
        </view>
      </view>

    </view>
  </view>

  <!-- 🌟 勋章馆弹窗 -->
  <view class="badge-modal-backdrop {{showBadgeModal ? 'show' : ''}}" bindtap="onCloseBadgeModal">
    <view class="badge-modal-sheet" catchtap="noBubble">
      
      <view class="sheet-header flex justify-between align-center">
        <view class="flex align-center" style="gap: 10rpx;">
          <text style="font-size: 36rpx;">🏅</text>
          <text class="sheet-title">自律成就勋章馆</text>
        </view>
        <text class="sheet-close" bindtap="onCloseBadgeModal">✕</text>
      </view>
      <text class="sheet-subtitle">每一次打卡与投喂，都在见证你与搭子的蜕变！</text>

      <view class="badge-grid-list">
        <view wx:for="{{badgeList}}" wx:key="id" 
              class="badge-card-item {{item.unlocked ? 'unlocked' : 'locked'}}" 
              bindtap="onTapBadgeItem" data-item="{{item}}">
          
          <view class="badge-icon-wrap">
            <text class="badge-emoji">{{item.icon}}</text>
            <view class="badge-lock-tag" wx:if="{{!item.unlocked}}">🔒</view>
            <view class="badge-check-tag" wx:if="{{item.unlocked}}">✓</view>
          </view>
          
          <text class="badge-name">{{item.name}}</text>
          <text class="badge-req">{{item.unlocked ? '已解锁' : item.req}}</text>
        </view>
      </view>

      <view class="badge-detail-card flex align-center" wx:if="{{selectedBadge}}">
        <text style="font-size: 54rpx; margin-right: 18rpx;">{{selectedBadge.icon}}</text>
        <view class="flex flex-col flex-1">
          <view class="flex align-center" style="gap: 10rpx;">
            <text style="font-size: 28rpx; font-weight: 900; color: #0F172A;">{{selectedBadge.name}}</text>
            <view class="badge-status-pill {{selectedBadge.unlocked ? 'unlocked' : 'locked'}}">
              {{selectedBadge.unlocked ? '✨ 达成成就' : '🔒 待解锁'}}
            </view>
          </view>
          <text style="font-size: 22rpx; color: #64748B; margin-top: 6rpx;">{{selectedBadge.desc}}</text>
        </view>
      </view>

    </view>
  </view>

  <!-- 🌟 成长蜕变庆祝全屏弹窗 -->
  <view class="evo-modal-backdrop {{showEvolutionModal ? 'show' : ''}}">
    <view class="evo-modal-card flex flex-col align-center">
      <view class="evo-fireworks">🌸✨💖✨🌸</view>
      <text class="evo-title">恭喜搭子成长蜕变！</text>
      <text class="evo-subtitle">自律的力量让它与你一同绽放光彩！</text>

      <view class="evo-stage-avatar-box">
        <view class="evo-radiant-glow"></view>
        <image class="evo-pet-avatar" src="{{activePetImage}}" mode="aspectFit"></image>
      </view>

      <view class="evo-rank-badge">
        <text style="font-size: 28rpx; font-weight: 900; color: #FFFFFF;">🌸 {{currentStageInfo.name}} · {{currentStageInfo.stageTitle}} 🌸</text>
      </view>
      <text class="evo-quote">“{{currentStageInfo.quote}}”</text>

      <button class="btn-evo-confirm" bindtap="onCloseEvolutionModal">
        💖 收到！继续保持自律好习惯
      </button>
    </view>
  </view>


  <!-- 🎨 3D 沉浸式场景切换 Sheet 弹窗 -->
  <view class="scene-modal-backdrop {{showSceneModal ? 'show' : ''}}" bindtap="onCloseSceneModal">
    <view class="scene-modal-sheet" catchtap="true">
      <view class="sheet-header flex align-center justify-between">
        <view class="flex align-center" style="gap: 12rpx;">
          <text style="font-size: 32rpx;">🎨</text>
          <text class="sheet-title">更换搭子生活场景</text>
        </view>
        <view class="sheet-close" bindtap="onCloseSceneModal">✕</view>
      </view>
      <text class="sheet-subtitle">为你的自律搭子挑选一个心仪的温馨生活空间，打卡陪伴更治愈</text>

      <view class="scene-cards-list">
        <view wx:for="{{sceneList}}" wx:key="id" 
              class="scene-card-item {{currentSceneId === item.id ? 'active' : ''}}"
              bindtap="onSelectScene" data-id="{{item.id}}">
          
          <view class="scene-thumb-box">
            <image class="scene-thumb-img" src="{{item.image}}" mode="aspectFill" />
            <view class="scene-active-badge" wx:if="{{currentSceneId === item.id}}">
              <text>✓ 使用中</text>
            </view>
            <view class="scene-tag-pill">{{item.tag}}</view>
          </view>

          <view class="scene-info-box flex flex-col justify-center">
            <view class="flex align-center" style="gap: 8rpx;">
              <text style="font-size: 28rpx;">{{item.icon}}</text>
              <text class="scene-title">{{item.name}}</text>
            </view>
            <text class="scene-desc">{{item.desc}}</text>
          </view>
        </view>
      </view>

    </view>
  </view>

</view>